# Xcode project is generated by XcodeGen from project.yml — don't commit it.
# Run `xcodegen generate` after cloning to recreate it.
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
*.moved-aside
*.hmap
*.ipa
*.xcuserstate

# Swift Package Manager
.build/
.swiftpm/

# macOS
.DS_Store

# CocoaPods / Carthage (unused here, ignored just in case)
Pods/
Carthage/Build/
